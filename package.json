{
  "name": "supabase-diff-action",
  "version": "0.1.0",
  "description": "GitHub Action to detect differences between Supabase environments (Edge Functions, RLS, SQL Functions, Schemas)",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "test": "vitest",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "supabase",
    "github-action",
    "diff",
    "rls",
    "postgres",
    "edge-functions"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/pg": "^8.10.9",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.3.2",
    "vitest": "^1.0.4"
  }
}
